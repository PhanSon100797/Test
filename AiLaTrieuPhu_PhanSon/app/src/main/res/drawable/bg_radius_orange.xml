<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring"
    android:innerRadius="20dp"
    android:thickness="5dp"
    android:useLevel="false">

    <stroke android:color="@color/colorOrange" android:width="1dp"
        android:dashWidth="0dp" android:dashGap="10dp"/>
<!--    <solid android:color="@color/colorGreen"/>-->
    //mau do
    <gradient
        android:startColor="@color/colorBlack"
        android:centerColor="@color/colorOrangeBlack"
        android:endColor="@color/colorOrange"
        android:type="sweep"
        android:angle="135"/>
        //android:type="sweep"

    //angle phai chia het cho 45
</shape>